import { Meteor } from 'meteor/meteor';
import '../imports/api/Teams';


Meteor.startup(() => {
  // code to run on server at startup
  // console.log('toto')
  // const numberOfRecords = Teams.find({}).count()
  // console.log(numberOfRecords)
});
